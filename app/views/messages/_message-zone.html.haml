// メッセージ表示部分の部分テンプレート
%p.chates__user
  = message.user.name
%p.chates__time
  = l(message.created_at,format: :default)
%p.chates__message
  = message.body
%p.chates__image
  = image_tag message.image if message.image_url.present?
%p.chates__id
  = message.id
